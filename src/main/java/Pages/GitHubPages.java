package Pages;

import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;

import static ObjectsPath.GitHubPaths.*; // star instead of the element name to work in all elements

public class GitHubPages {

}
